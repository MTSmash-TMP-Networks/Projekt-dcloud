Flask>=3.0,<4.0
PyYAML>=6.0,<7.0
cryptography>=42.0,<46.0
impacket>=0.12.0,<1.0


# Optional system dependency for dcloud Web/PHP hosting:
# Install php-cgi or php and make it available on PATH.
# This cannot be installed by pip, so it is documented here instead of being
# listed as a Python package that would break `pip install -r requirements.txt`.
